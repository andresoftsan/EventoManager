# Database Configuration
DATABASE_URL=postgresql://usuario:senha@localhost:5432/workday

# Session Secret (generate a strong random string)
SESSION_SECRET=sua_chave_secreta_muito_longa_e_segura_aqui

# Application Configuration
NODE_ENV=development
PORT=5000

# PostgreSQL Environment Variables (if using separate config)
PGHOST=localhost
PGPORT=5432
PGDATABASE=workday
PGUSER=usuario
PGPASSWORD=senha

# External API Configuration
EXTERNAL_API_KEY=sua_chave_api_externa_super_secreta_aqui